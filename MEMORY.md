# Memoria del progetto / Project memory

Aggiornato / Updated: 2026-09-26. Questa memoria conserva decisioni e problemi aperti; prove e stato sono nelle matrici [M0 IT](DOCS/it/M0-VALIDATION.md) / [EN](DOCS/en/M0-VALIDATION.md), [M1 IT](DOCS/it/M1-VALIDATION.md) / [EN](DOCS/en/M1-VALIDATION.md) e [M2 IT](DOCS/it/M2-VALIDATION.md) / [EN](DOCS/en/M2-VALIDATION.md).

## Italiano

### Requisiti confermati dall'autore

- Autore: **Matteo Luigi Feroldi**; repository [Matte2599/WebFence](https://github.com/Matte2599/WebFence). Applicazione desktop nativa e documentazione IT/EN; futuri report IT/EN.
- Target minimi M1 decisi dall'autore: macOS **26** solo Apple Silicon ARM64; Windows **10 1809+** e 11 x86-64 (limite Qt 6.11); Ubuntu **24.04 LTS** x86-64/ARM64. Altre Debian/derivate sono obiettivo di compatibilità, non baseline M1. Questi target non sono tutti provati su macchine minime reali ([policy IT](DOCS/it/M1-SUPPORT-POLICY.md)). UX sobria e familiare come strumenti Windows 2015–2020, semplice per principianti e dettagliata per esperti.
- Go e Qt Widgets/MIQT sono scelte dell'autore dopo confronto con Rust/Fyne ([ADR-001 IT](DOCS/it/ADR-001-LANGUAGE.md), [ADR-002 IT](DOCS/it/ADR-002-GUI.md)). Cambi architetturali richiedono un ADR.
- **WebFence Community License 1.0** è source-available, non open source OSI: persone e professionisti indipendenti possono usarla gratuitamente anche per analisi retribuite; aziende richiedono autorizzazione anche per uso interno. Rivendita/distribuzione commerciale del software richiede accordo dell'autore, eventualmente con royalties. [Analisi legale interna M1](DOCS/it/M1-LEGAL-ASSESSMENT.md) completata; un parere professionale e la verifica dei singoli pacchetti restano da organizzare prima degli usi pertinenti.
- Obiettivo di lungo periodo: analisi tradizionale e AI opzionale, CVE aggiornate, report firmati, progetti persistenti, convalida fix e regressioni. Invicti/Acunetix sono riferimenti di profondità desiderata, non equivalenza dimostrata.
- Flusso di lavoro richiesto: branch dedicato per blocco, CI verde, squash merge su `main`, verifica CI del commit finale. Non creare commit solo per registrare CI. Aggiornare questa memoria alla fine del blocco; le istruzioni correnti dell'autore prevalgono.

### Decisioni tecniche da conservare

- Il laboratorio M0 di 10.000 fixture resta separato dalla scansione. La [alpha tecnica M1](DOCS/it/M1-VALIDATION.md) collega progetto dichiarato/autorizzato, run gestita, broker HTTP(S) controllato, discovery HTML osservativa, controllo `HTTP-XCTO-001`, ledger redatto e GUI IT/EN. È un primo controllo di header, non un audit generale. Nessun LLM è richiesto. La GUI M1 supporta una origine e un seed per scansione; il core ne supporta più entro limiti espliciti.
- Il primo [blocco core M2](DOCS/it/M2-INTELLIGENCE-CACHE.md) aggiunge cache SQLite CVE/NVD separata, sync NVD transazionale per finestre esplicite e recupero CVE per ID, con hash/provenienza e stato di aggiornamento. Le prove locali sintetiche di paginazione, rollback e riapertura sono superate; non è stata fatta una sincronizzazione reale né un'importazione di catalogo completo.
- Il [matching core M2](DOCS/it/M2-MATCHING.md) richiede segnali prodotto/versione espliciti e mantiene `unknown` per condizioni o schemi non supportati; non deriva CVE dagli header M1. Backport e verifica richiedono attestazioni esplicite, non prove automatiche. Le fixture sintetiche e le regressioni locali sono superate; non esiste ancora una misura di accuratezza su corpus reale.
- La [simulazione extra M2](DOCS/it/M2-ACCURACY-SIMULATION.md) misura solo 30 casi sintetici: 17 conclusioni corrette su 20 casi con verità nota, tre astensioni e dieci casi indeterminati. Per NVD senza CPE completo serve ora la parte `a`/`o`/`h`; qualificatori CVE non dimostrati e versioni discordanti restano `unknown`. Non è una misura di accuratezza reale.
- Il [core report M2](DOCS/it/M2-REPORTS.md) esporta una run conclusa in JSON/HTML IT/EN con manifest JCS, JWS Ed25519 e verifica offline a fiducia esterna. La firma usa una chiave dell'operatore nel portachiavi nativo; rotazione e revoca agiscono sul registro pubblico locale. Le prove sintetiche di alterazione e regressione locale sono superate, ma la fiducia offline non garantisce revoca corrente né orario attestato.
- Il [desktop M2 esteso](DOCS/it/M2-GUI-EXTENSION.md) espone aggiornamento manuale NVD/CVE, parte/CPE completo, backport e verifica attestati, stato di entrambe le fonti, export/verifica e gestione locale delle chiavi pubbliche. Le attestazioni nel form si azzerano quando cambia segnale/CVE/run; le correlazioni GUI sono effimere fino all'export. La chiave privata resta nel portachiavi; le regressioni Qt con portachiavi sintetico e la resa Cocoa/offscreen IT/EN sono state verificate localmente. Non derivare CVE dagli header M1 e non fare download feed all'avvio.
- Il [primo blocco core M3](DOCS/it/M3-BROWSER-GATE.md) aggiunge un gate per richieste browser legato a run gestita: scope/percorso, tipi ammessi, budget, concorrenza e revoca. Non esiste ancora un adattatore browser; worker e WebSocket sono negati. Un interceptor non prova contenimento dell'egress o limiti di processo.
- Il [secondo blocco core M3](DOCS/it/M3-BROWSER-PROXY.md) aggiunge un proxy HTTP su loopback con credenziale effimera, che passa dal gate e dal broker con IP fissati ([ADR-009](DOCS/it/ADR-009-BROWSER-HTTP-BOUNDARY.md)). Le prove sintetiche `httptest` e le regressioni locali sono passate; HTTPS CONNECT, cookie e sessioni non sono implementati. Il proxy non forza un processo esterno a usarlo.
- Il [laboratorio browser M3](DOCS/it/M3-BROWSER-LAB.md) usa Qt WebEngine/MIQT con profilo effimero, interceptor e proxy su fixture locali ([ADR-010](DOCS/it/ADR-010-QT-BROWSER-LAB.md)). La prova locale macOS ha superato documento, script, `fetch`, subresource e redirect esclusi; il job CI macOS è distinto e non va dichiarato verde senza run pertinente. Non è una funzionalità desktop o un isolamento di processo dimostrato.
- Richiedere consenso dichiarato, origine esatta, policy GET/percorso, grant IP esatti, budget e cadenza; seguire i link solo con opt-in, mai inviare form. La modalità pubblica richiede IP pubblici fissati manualmente ([ADR-008 IT](DOCS/it/ADR-008-PINNED-PUBLIC-TRANSPORT.md)); i test non hanno contattato target esterni. Trattare pagine, DNS e futuri output AI come dati non fidati. La dichiarazione dell'operatore non prova la sua autorizzazione.
- SQLite **v4** salva dichiarazioni/revisioni e solo codici/conteggi di run/visite, senza URL visitati, query, header, body o credenziali ([store IT](DOCS/it/M1-PROJECT-STORE.md)). Lock OS esclusivo sul file, una run crawler persistente per istanza, marcatura `interrupted` al riavvio, limite di 64 MiB sul file principale, 100 run per progetto e 256 visite per run, backup privato in file nuovo, eliminazione logica a cascata. WAL/backup esterni non sono cancellati in modo forense. Il riferimento di autorizzazione e le origini restano metadati in chiaro; non inserirvi segreti.
- JWS Ed25519 e portachiavi nativi sono collegati al report M2; il packaging di sviluppo contiene il desktop con gestione chiavi, ma non gli ausili CLI né costituisce una release supportata. Il bundle macOS applica la correzione Qt Cocoa ([ADR-006 IT](DOCS/it/ADR-006-QT-COCOA.md)); Windows CI usa il lock dei pacchetti MSYS2 ([guida IT](DOCS/it/DEVELOPMENT.md)).
- Non commettere segreti, chiavi private, dati clienti, pesi, database CVE o report reali. Non eseguire scansioni esterne senza target e attività esplicitamente autorizzati nella richiesta.

### Problemi aperti

- **M1 è chiusa come alpha tecnica controllata** per decisione dell'autore del 25 settembre 2026: i test locali/CI sostituiscono i precedenti gate umani come criterio di milestone ([matrice IT](DOCS/it/M1-VALIDATION.md)). Le prove H01…H05 e sulle versioni minime reali non sono superate; servono per eventuali dichiarazioni di supporto/accessibilità. Il parere professionale e un CLA attivo restano condizioni per distribuzione/contratti o contributi pertinenti, non gate M1. Mac locale Apple Silicon/macOS 26.6.2 con un display; Windows 10 dell'autore disponibile ma senza prova documentata.
- Il broker pubblico non è stato provato su rete pubblica reale. Archivi separati non condividono un limite globale; manca feedback sul carico del target e un firewall egress indipendente. La policy non interpreta query né garantisce che GET sia senza effetti. La GUI non offre ancora rinnovo/revoca, backup o ripristino guidato.
- La GUI M2 non conserva correlazioni dopo la sessione né offre recupero guidato. La cache e il matching coprono solo il sottoinsieme documentato, senza catalogo completo, test CVE attivi o misura di accuratezza reale. La verifica grafica locale non sostituisce prove assistive e hardware su tutti i target. Browser/autenticazione, retest e AI restano futuri. Contributi esterni non concedono automaticamente diritti di rilicenza ([CONTRIBUTING](CONTRIBUTING.md)).
- Per M3 restano da scegliere e integrare runtime browser, egress indipendente con IP fissati, limiti di processo, fixture SPA/API, sessioni e controlli tra ruoli prima di considerare completata la prima voce della roadmap.
- Il proxy M3 ammette solo HTTP GET/HEAD e non conserva né inoltra cookie: le SPA con login non sono ancora coperte. Servono prove negative con un browser reale su ciascun sistema prima di abilitarlo nel desktop.
- Il laboratorio Qt gira nel processo di prova e non applica egress indipendente, limite processi/memoria o mediazione HTTPS; un helper separato e la revisione packaging/licenze Qt WebEngine restano aperti.

## English

### Author-confirmed requirements

- Author: **Matteo Luigi Feroldi**; repository [Matte2599/WebFence](https://github.com/Matte2599/WebFence). Native desktop application and IT/EN documentation; future IT/EN reports.
- Author-decided M1 target minimums: macOS **26** on Apple Silicon ARM64 only; Windows **10 1809+** and 11 x86-64 (Qt 6.11 floor); Ubuntu **24.04 LTS** x86-64/ARM64. Other Debian distributions/derivatives are a compatibility goal, not the M1 baseline. These targets have not all been tested on real minimum-version machines ([EN policy](DOCS/en/M1-SUPPORT-POLICY.md)). Restrained, familiar 2015–2020 Windows-tool UX, accessible to novices with detail for experts.
- Go and Qt Widgets/MIQT are author-selected after Rust/Fyne comparison ([ADR-001 EN](DOCS/en/ADR-001-LANGUAGE.md), [ADR-002 EN](DOCS/en/ADR-002-GUI.md)). Architecture changes require an ADR.
- **WebFence Community License 1.0** is source-available, not OSI open source: individuals and independent professionals may use it free of charge, including for paid assessments; companies need authorization even for internal use. Software resale/commercial distribution needs the author's agreement, potentially with royalties. The [M1 internal legal assessment](DOCS/en/M1-LEGAL-ASSESSMENT.md) is complete; a professional opinion and artifact-specific compliance review remain to be arranged before relevant uses.
- Long-term goal: traditional and optional AI analysis, updated CVEs, signed reports, persistent projects, fix validation and regressions. Invicti/Acunetix are desired depth references, not established equivalence.
- Requested workflow: dedicated branch per block, green CI, squash merge into `main`, verify CI for the final commit. Do not make CI-status-only commits. Update this memory at block end; current author instructions prevail.

### Technical decisions to preserve

- The 10,000-fixture M0 lab stays separate from scanning. The [technical M1 alpha](DOCS/en/M1-VALIDATION.md) connects an operator-declared/authorized project, managed run, controlled HTTP(S) broker, observational HTML discovery, `HTTP-XCTO-001` check, redacted ledger and IT/EN GUI. This is one initial header check, not a general audit. No LLM is required. The M1 GUI supports one origin and seed per scan; the core supports more within explicit bounds.
- The first [M2 core block](DOCS/en/M2-INTELLIGENCE-CACHE.md) adds a separate SQLite CVE/NVD cache, transactional NVD synchronization for explicit windows and CVE lookup by ID, with hashes/provenance and freshness status. Synthetic local pagination, rollback and reopen tests passed; no real feed sync or full catalog import was performed.
- [M2 core matching](DOCS/en/M2-MATCHING.md) requires explicit product/version signals and keeps `unknown` for unsupported conditions or schemes; it does not infer CVEs from M1 headers. Backports and verification require explicit attestations, not automatic proof. Synthetic fixtures and local regressions passed; no real-corpus accuracy measure exists yet.
- The [extra M2 simulation](DOCS/en/M2-ACCURACY-SIMULATION.md) measures only 30 synthetic cases: 17 correct conclusions among 20 known-truth cases, three abstentions and ten indeterminate cases. NVD without a full CPE now requires the `a`/`o`/`h` part; unproven CVE qualifiers and conflicting versions remain `unknown`. This is not a real-world accuracy measure.
- The [M2 report core](DOCS/en/M2-REPORTS.md) exports a finished run to IT/EN JSON/HTML with a JCS manifest, Ed25519 JWS and offline verification using external trust. Signing uses an operator key in the native credential store; rotation and revocation act on the local public registry. Synthetic tampering and local regression tests passed, but offline trust does not guarantee current revocation or an attested clock.
- The [extended M2 desktop](DOCS/en/M2-GUI-EXTENSION.md) exposes manual NVD/CVE updates, CPE part/full CPE, attested backport and verification, both source states, export/verification and local public key management. Form attestations reset when the signal/CVE/run changes; GUI assessments remain ephemeral until export. The private key stays in the native credential store; Qt regressions with a synthetic key store and Cocoa/offscreen IT/EN rendering were checked locally. Never infer CVEs from M1 headers or download feeds on startup.
- The [first M3 core block](DOCS/en/M3-BROWSER-GATE.md) adds a managed-run browser request gate: scope/path, admitted types, budget, concurrency and revocation. There is no browser adapter yet; workers and WebSockets are denied. An interceptor does not prove egress containment or process limits.
- The [second M3 core block](DOCS/en/M3-BROWSER-PROXY.md) adds a loopback HTTP proxy with an ephemeral credential, traversing the gate and pinned broker ([ADR-009](DOCS/en/ADR-009-BROWSER-HTTP-BOUNDARY.md)). Synthetic `httptest` cases and local regressions passed; HTTPS CONNECT, cookies and sessions are not implemented. The proxy cannot force an external process to use it.
- The [M3 browser lab](DOCS/en/M3-BROWSER-LAB.md) uses Qt WebEngine/MIQT with an ephemeral profile, interceptor and proxy on local fixtures ([ADR-010](DOCS/en/ADR-010-QT-BROWSER-LAB.md)). The local macOS trial passed document, script, `fetch`, and blocked subresource/redirect cases; the macOS CI job is separate and must not be called green without its run. This is neither a desktop feature nor proven process isolation.
- Require declared consent, exact origin, GET/path policy, exact IP grants, budgets and pacing; follow links only with opt-in and never submit forms. Public mode needs manually pinned public IPs ([ADR-008 EN](DOCS/en/ADR-008-PINNED-PUBLIC-TRANSPORT.md)); tests contacted no external target. Treat pages, DNS and future AI output as untrusted. An operator declaration does not prove authorization.
- SQLite **v4** stores declarations/revisions and only run/visit codes and counts, without visited URLs, queries, headers, bodies or credentials ([store EN](DOCS/en/M1-PROJECT-STORE.md)). Exclusive OS file lock, one persistent crawler run per instance, `interrupted` marking at restart, 64 MiB main-file cap, 100 runs per project and 256 visits per run, private backup to a new file and cascading logical deletion. WAL/separate backups are not forensically erased. Authorization references and origins remain plaintext metadata; do not enter secrets.
- Ed25519 JWS and native keychains are connected to M2 reports; development packages contain the desktop with key management, but not the CLI helpers, and are not supported releases. The macOS bundle applies the Qt Cocoa correction ([ADR-006 EN](DOCS/en/ADR-006-QT-COCOA.md)); Windows CI uses the MSYS2 package lock ([EN guide](DOCS/en/DEVELOPMENT.md)).
- Do not commit secrets, private keys, client data, weights, CVE databases or real reports. Do not scan external systems without targets and activities explicitly authorized in the request.

### Open problems

- **M1 is closed as a controlled technical alpha** by the author's September 25, 2026 decision: local/CI tests replace the earlier human gates as milestone criteria ([EN matrix](DOCS/en/M1-VALIDATION.md)). H01…H05 and real minimum-version trials have not passed; they remain necessary for any corresponding support/accessibility claims. Professional advice and an active CLA remain conditions for relevant distribution/contracts or contributions, not M1 gates. Local Mac: Apple Silicon/macOS 26.6.2 with one display; the author's Windows 10 PC is available but has no documented trial.
- Public transport has not been tested on a real public network. Separate stores do not share an overall limit; target load feedback and independent egress firewall are missing. Policy does not interpret queries or guarantee that GET has no effects. The GUI does not yet offer authorization renewal/revocation, backup or guided restore.
- The M2 GUI does not persist assessments across sessions or offer guided recovery. The cache and matching cover only the documented subset, without a full catalog, active CVE checks or a real-world accuracy measure. Local visual checks do not replace assistive and hardware trials on every target. Browser/authentication, retests and AI remain future work. External contributions do not automatically grant relicensing rights ([CONTRIBUTING](CONTRIBUTING.md)).
- M3 still needs a selected and integrated browser runtime, independent egress with pinned IPs, process limits, SPA/API fixtures, sessions and cross-role checks before the roadmap's first criterion can be considered complete.
- The M3 proxy allows HTTP GET/HEAD only and neither stores nor forwards cookies: SPAs with login are not yet covered. Negative tests with a real browser on each platform are needed before enabling it in the desktop.
- The Qt lab runs in the test executable's process and applies no independent egress, process/memory limit or HTTPS mediation; a separate helper and Qt WebEngine packaging/license review remain open.
