# M0 — Verifica Git offline di winpthreads / Offline winpthreads Git verification

[Fonti IT](../it/M0-WINDOWS-SOURCES.md) · [Sources EN](../en/M0-WINDOWS-SOURCES.md) · [Matrice IT](../it/M0-VALIDATION.md) · [Matrix EN](../en/M0-VALIDATION.md)

## Italiano

**Prova locale del 2026-09-24; CI del codice in attesa.** Il lock VCS `packaging/windows/winpthreads-vcs-lock.json`, SHA-256 `b1c389219a3be61fab191cbe32c8d75b75ef991073e2847887c6da8e11b7e7e1`, fissa versione/ricetta MSYS2, hash dell'archivio sorgente, commit Git, tre file pack/index/rev e hash/dimensione del tar atteso da `.SRCINFO`. I valori sono stati ricontrollati contro il [supplemento manuale precedente](windows-sources-2026-09-23/winpthreads-offline-check.json). Il collector accetta soltanto l'archivio esatto, copia i **tre file oggetto** in una repository bare temporanea inizializzata da Git, disabilita configurazioni Git ambientali e sostituzioni di oggetti, esegue `git fsck --strict --full COMMIT` e produce `git -c core.abbrev=no archive --format=tar COMMIT`. Non copia config, ref o hook dall'archivio; non effettua checkout, fetch o esecuzione di ricette. Il tar di **140.103.680 byte** ha SHA-256 `ee1989c086f380e53663ecaebd932b793b59b1a2b660ffc035723df8640559a5`, uguale a `.SRCINFO`.

La [nuova raccolta locale](windows-vcs-2026-09-24/source-materials.json), SHA-256 `3b186ca368afc2760435b6f6037dc01d453496c003ab87fe046c7871bc2bf940`, è stata ricostruita riusando i **21 archivi originali/316.641.108 byte** della raccolta del 2026-09-23, senza scaricarli di nuovo. Identità, ricette, hash e file degli altri archivi sono invariati. Ora risultano **104 input verificati** (incluso Git) e **otto input `no_strong_checksum`** relativi a firme `SKIP`; nessun VCS resta `vcs_unverified` in questa versione del piano. Il [nuovo allegato locale](windows-vcs-2026-09-24/attachment.json), SHA-256 `35fe1f273da56c549e5fcc3bc90a9872c7c0d2ec38870a4c3a35a7e3323c8edb`, ha riletto gli archivi, ricontrollato Git e incluso il lock. La vecchia raccolta `vcs_unverified` è stata rifiutata senza pubblicare un allegato. I 67 test Python locali passano, inclusa una repository Git sintetica e casi alterati. Le copie degli archivi e i tar temporanei restano fuori Git.

**Limite:** la corrispondenza del tar Git con il checksum della ricetta prova l'integrità rispetto all'archivio e alla ricetta MSYS2 vincolati, non autentica con firma PGP l'origine esterna. Le otto firme upstream con `SKIP`, i componenti incorporati, le licenze, la ricompilazione completa e i desktop reali restano da verificare. `distribution_ready=false` e `corresponding_sources_complete=false`; M0-02/M0-06 aperti.

## English

**Local trial on 2026-09-24; changed-code CI pending.** The VCS lock `packaging/windows/winpthreads-vcs-lock.json`, SHA-256 `b1c389219a3be61fab191cbe32c8d75b75ef991073e2847887c6da8e11b7e7e1`, pins the MSYS2 version/recipe, source archive hash, Git commit, three pack/index/rev files and the tar hash/size declared in `.SRCINFO`. Values were rechecked against the [earlier manual supplement](windows-sources-2026-09-23/winpthreads-offline-check.json). The collector accepts only the exact archive, copies the **three object files** into a temporary bare repository initialized by Git, disables ambient Git configuration and object replacements, runs `git fsck --strict --full COMMIT`, then produces `git -c core.abbrev=no archive --format=tar COMMIT`. It does not copy archived config, refs or hooks, check out a worktree, fetch, or execute recipes. The **140,103,680-byte** tar has SHA-256 `ee1989c086f380e53663ecaebd932b793b59b1a2b660ffc035723df8640559a5`, matching `.SRCINFO`.

The [new local collection](windows-vcs-2026-09-24/source-materials.json), SHA-256 `3b186ca368afc2760435b6f6037dc01d453496c003ab87fe046c7871bc2bf940`, was rebuilt by reusing the **21 original archives/316,641,108 bytes** from the 2026-09-23 collection, without another download. Identities, recipes, hashes and files in the other archives are unchanged. It now records **104 verified inputs** (including Git) and **eight `no_strong_checksum` inputs** for `SKIP` signatures; none remains `vcs_unverified` in this plan version. The [new local attachment](windows-vcs-2026-09-24/attachment.json), SHA-256 `35fe1f273da56c549e5fcc3bc90a9872c7c0d2ec38870a4c3a35a7e3323c8edb`, reread the archives, rechecked Git and included the lock. The old `vcs_unverified` collection was rejected without publishing an attachment. All 67 local Python tests passed, including a synthetic packed Git repository and tampered cases. Original archives and temporary tar files remain outside Git.

**Limit:** matching the Git tar with the recipe checksum proves integrity relative to the locked MSYS2 archive and recipe, not PGP-authenticated external origin. Eight upstream signatures marked `SKIP`, embedded components, licenses, full rebuilding and real desktops still require review. `distribution_ready=false` and `corresponding_sources_complete=false`; M0-02/M0-06 remain open.
