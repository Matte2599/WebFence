# M0 — Righe AX dopo il reset dei filtri / AX rows after filter reset

[Matrice IT](../it/M0-VALIDATION.md) · [Matrix EN](../en/M0-VALIDATION.md) · [Cocoa ADR IT](../it/ADR-006-QT-COCOA.md) · [Cocoa ADR EN](../en/ADR-006-QT-COCOA.md)

## Italiano

**Stato: difetto aperto in M0-01.** Il 2026-09-24, su macOS 26.6.2 Apple Silicon, è stata esaminata una **copia privata** del bundle storico `3598b5f` con la patch Cocoa corrente, RPATH esterni rimossi e firma ad hoc verificata. L'app normale è stata avviata con `HOME` temporanea e sole fixture sintetiche. Il client AX aveva già accesso (`AXIsProcessTrusted=true`); nessuna preferenza di sistema è stata modificata. Il bundle originale è rimasto firmato e l'hash della preferenza lingua personale è rimasto `d2bc81b3c6edc55e4b775d89ba679708610c7a417d45c696819ea8a73620ca82`.

La sequenza controllata dall'API macOS `AXUIElement` è stata ripetuta su due avvii con stati lasciati fermi almeno 0,35–1 s:

| Passo | Tabella AX | Prima riga |
| --- | --- | --- |
| Attivare «Carica 10.000 esempi» con `AXPress` | 10.000 righe, 4 colonne | `AXRow`, 4 `AXCell`, ID `DEMO-00001` |
| Impostare filtro `DEMO-10000` via `AXValue` | 1 riga, 4 colonne | `AXRow`, prima cella `DEMO-10000` |
| Impostare `no-such-fixture` | 0 righe, 4 colonne | Nessuna riga |
| Svuotare il filtro | 10.000 righe, 4 colonne | Lettura di `AXRole` della prima riga: **−25202**, `kAXErrorInvalidUIElement` |

Una nuova istanza del client AX ha confermato il riferimento invalido mentre l'app era ferma. Impostare il focus AX sulla tabella è riuscito ma non lo ha ripristinato. Il self-test interno `QAccessibleTableInterface` continua a passare dopo lo stesso filtro; pertanto quel test non dimostra la validità dei riferimenti pubblicati dal bridge del sistema. Un soak Cocoa da 30 s/10 cicli con letture AX durante il ciclo è terminato senza crash, ma le letture durante reset rapidi non sono una verifica atomica della riga.

Sono state compilate e firmate **solo copie private** del plugin Cocoa per isolare tre ipotesi: identità/hash distinti delle righe sintetiche; notifica `NSAccessibilityRowCountChangedNotification` al reset; restituzione diretta dell'array di righe sintetiche. Le prime due hanno superato il self-test interno ma **non** hanno corretto il riferimento AX invalido. La terza è stata provata mentre l'attivazione dell'app non era stabile: l'albero AX anomalo e i fallimenti di focus non possono essere attribuiti con certezza alla modifica. Nessuna variante è entrata nel repository o nel bundle originale. In prove successive la stessa sessione AX ha talvolta restituito `AXApplication` al posto di `AXWindow` per nuovi processi. Un controllo UI successivo ha segnalato che il Mac era bloccato; il momento esatto del blocco non è noto. Perciò le anomalie delle prove tardive sono inconcludenti. Il difetto delle prime due riproduzioni va ricontrollato a schermo sbloccato prima di decidere una correzione.

La prossima verifica richiede ripetere la sequenza reset 10.000 → 1 → 0 → 10.000 con Mac sbloccato e sessione AX pulita; se il difetto persiste, isolare la durata dei riferimenti nativi e correggerlo prima del collaudo con lettore reale. Non chiudere M0-01 sulla base dei soli contatori di righe, del self-test Qt o del soak.

Il commit `4635117` aggiunge `scripts/test-macos-ax-reset.swift`: avvia il bundle con HOME temporanea e verifica, a stato fermo, ruolo e quattro celle della prima riga nelle quattro fasi. La compilazione con Swift 6.4 locale è riuscita; il bundle non è stato eseguito nella sessione Mac bloccata. La CI macOS esegue il test come diagnostica non bloccante: l'uscita 77 significa che il client non ha il permesso Accessibilità e **non** costituisce un risultato positivo. Anche un esito AX positivo del runner non sostituirà VoiceOver su desktop reale.

Nella [run `4635117`](https://github.com/Matte2599/WebFence/actions/runs/35954825284/job/107490798295), lo step AX sul runner macOS 15 **ha eseguito la prova**, senza uscita 77: quattro esiti `PASS` per 10.000/1/0/10.000 righe; nella fase finale la prima riga espone `AXRow`, quattro celle e `DEMO-00001`. La run completa ha concluso con **sei job verdi**. Questo valida il bridge del bundle CI in quel contesto, non risolve la discrepanza con la prova locale: cambiano sia la versione macOS (15 contro 26.6.2) sia il bundle (commit corrente contro copia storica `3598b5f`). Il Mac locale risulta ancora bloccato; non si inferisce che il difetto sia corretto o specifico della versione OS.

Il confronto Git `3598b5f..41256ae` non mostra modifiche a `internal/desktop`, `scripts/qt-cocoa` o `scripts/build-qt-cocoa.sh`; in `scripts/package-macos.sh` cambiano solo due righe relative al controllo dei collegamenti. Questo riduce le differenze di sorgente UI/plugin fra le prove, ma non rende identici i binari, gli ambienti o le sessioni AX.

Nella [CI `41256ae`, run 35956499419](https://github.com/Matte2599/WebFence/actions/runs/35956499419), **tutti e sette i job sono verdi**, inclusi i bundle, self-test Cocoa, sostituzione del plugin e materiali nativi sui due runner macOS. La diagnostica AX è però `continue-on-error` e **fallisce su entrambi**: [macOS 15](https://github.com/Matte2599/WebFence/actions/runs/35956499419/job/107495836991) ottiene `PASS` per 10.000, 1 e 0 righe, poi `AXError -25202` leggendo il ruolo della prima riga ripristinata; [macOS 26](https://github.com/Matte2599/WebFence/actions/runs/35956499419/job/107495837015) ottiene `PASS` per 10.000 righe ma, filtrando a una, trova quattro celle con titolo ID vuoto invece di `DEMO-10000`. In entrambi il client AX ha eseguito la prova, senza uscita 77. Il successo delle altre verifiche non è un successo di accessibilità; il passaggio precedente su macOS 15 non è riproducibile con costanza. Il prossimo passo è isolare durata/valore delle celle e ripetere con Mac sbloccato, poi correggere il bridge prima di chiudere M0-01.

Nel commit `b66aefd` la diagnostica esegue tre cicli di filtro e fotografa gli attributi AX dopo ogni errore. Sul [runner macOS 15](https://github.com/Matte2599/WebFence/actions/runs/35959021694/job/107503379369), il test parte da 10.000 righe valide. Al primo ciclo passano i filtri a una e zero righe, ma fallisce l'ID della prima cella dopo il ripristino: `AXRows=10000` e quattro celle, con ruolo/titolo/valore delle celle tutti `AXError -25202`. Al secondo ciclo fallisce anche il ruolo della riga filtrata a un risultato (`-25202`); dopo il filtro vuoto passa e il ripristino ripete la cella invalida. Al terzo ciclo passano i filtri a uno e zero risultati ma fallisce ancora la cella dopo il ripristino. Quattro stadi falliti in totale, senza uscita 77. Il problema riguarda la validità dei riferimenti, non soltanto quale attributo testuale viene interrogato. La diagnostica non blocca il job generale.

Sul [runner macOS 26 della stessa run](https://github.com/Matte2599/WebFence/actions/runs/35959021694/job/107503379258) la tabella iniziale passa, ma **sei stadi su nove** falliscono nei tre cicli: già al primo filtro a una riga il titolo ID è vuoto e ruolo/titolo/valore della cella nel nuovo snapshot restituiscono `-25202`; al primo ripristino la riga stessa è invalida. Nei due cicli successivi sia la riga con un risultato sia quella ripristinata sono invalidi (`-25202`); i filtri senza risultati passano. Il client ha eseguito il test, senza uscita 77. I due OS mostrano lo stesso tipo di riferimento invalido, con frequenza diversa. Questa diagnostica usa ancora il modello precedente al proxy; l'efficacia della modifica applicativa resta da misurare.

La [CI `18c513e`, run 35961360935](https://github.com/Matte2599/WebFence/actions/runs/35961360935) prova il modello sorgente stabile con `QSortFilterProxyModel`: durante i filtri il self-test Qt conferma inserimenti/rimozioni di righe e nessun `ModelReset`. I pacchetti Debian, Windows e i due job Linux nativi passano. La diagnostica Cocoa **non migliora in modo verificato**: [macOS 15](https://github.com/Matte2599/WebFence/actions/runs/35961360935/job/107510426255) fallisce quattro stadi, [macOS 26](https://github.com/Matte2599/WebFence/actions/runs/35961360935/job/107510426348) sei, come nel confronto precedente. Su 15 il primo ripristino espone `AXRow` ma celle invalide; nei cicli successivi diventano invalidi anche i riferimenti di riga. Su 26 il primo filtro a una riga espone la riga ma celle invalide e il ripristino invalida la riga. Questi risultati escludono che sostituire solo il reset con i segnali delle righe basti a risolvere M0-01. Serve un'indagine mirata sul bridge Qt/Cocoa e una prova VoiceOver reale.

La [run `18c513e` completata al secondo tentativo](https://github.com/Matte2599/WebFence/actions/runs/35961360935) ha **sette job verdi**: il solo job macOS 26 del primo tentativo era fallito per timeout del mirror dei sorgenti, dopo le prove Cocoa. Il rilancio ha completato anche i materiali nativi. La [diagnostica AX macOS 26 rilanciata](https://github.com/Matte2599/WebFence/actions/runs/35961360935/job/107516319443) fallisce ancora **sei stadi su nove**, con la stessa sequenza di celle e righe `-25202`; resta non bloccante. CI verde non significa accessibilità verificata.

La [CI `2904f5f`, run 35965326044](https://github.com/Matte2599/WebFence/actions/runs/35965326044) confronta una variante **non distribuibile** di Cocoa in una copia firmata del bundle: `accessibilityRows` restituisce direttamente l'array di righe sintetiche. Il patch set sorgente si applica senza fuzz all'archivio Qt 6.11.2 verificato; i due runner compilano la variante, verificano firma, self-test e soak. Il self-test locale precedente restava inconcludente per il focus nella sessione Mac bloccata. Sul [runner macOS 15](https://github.com/Matte2599/WebFence/actions/runs/35965326044/job/107522517243), bundle normale e variante falliscono entrambi **tre stadi su nove**: le fasi precise differiscono, coerentemente con l'intermittenza già osservata. Sul [runner macOS 26](https://github.com/Matte2599/WebFence/actions/runs/35965326044/job/107522517116), entrambi falliscono **sei stadi su nove**, con la stessa sequenza: cella invalida al primo filtro a una riga, poi righe invalide nei ripristini e nei filtri successivi. Il client AX viene eseguito, senza uscita 77. La restituzione diretta delle righe **non risolve il difetto**; la variante resta disponibile solo come riproduzione manuale e la ricompilazione aggiuntiva è rimossa dalla CI ordinaria. Nessuna modifica al plugin di produzione è adottata, M0-01 resta aperto e serve una prova con Mac sbloccato e VoiceOver reale.

La [CI `a8b65f2`](https://github.com/Matte2599/WebFence/actions/runs/35966364447) è conclusa con sette job verdi dopo la rimozione della variante diretta; le due diagnostiche AX normali restano non bloccanti e falliscono. Il codice Qt 6.11.2 usa lo stesso `axid` per tutti gli elementi sintetici di una tabella; `isEqual` confronta solo `axid` e ruolo sintetico, mentre `hash` usa solo `axid`. Gli indici riga/colonna non partecipano all'identità nativa. La variante `--ax-distinct-identity` li aggiunge **solo in copia privata**. Il patch set combinato si applica senza fuzz; compilazione e firma locali riuscite, Go build ID e originali preservati. Il self-test locale falliva nei controlli focus durante la sessione bloccata; un soak Cocoa separato ha superato quattro cicli/12,006 s.

La [CI `0a568ff`, run 35967360307](https://github.com/Matte2599/WebFence/actions/runs/35967360307) ha **sette job verdi**. Sul [runner macOS 26](https://github.com/Matte2599/WebFence/actions/runs/35967360307/job/107528861229), la variante privata compila e supera firma, self-test e soak, ma il client AX a tre cicli fallisce **sei stadi su nove**, come il bundle normale nella stessa run: cella invalida al primo filtro a una riga, poi righe invalide dopo il ripristino e nei filtri successivi (`-25202`). Il client viene eseguito, senza uscita 77. `CFEqual` fra prima e ultima riga è già `false` nel bundle normale e nella variante; i valori `CFHash` sono distinti, quindi queste letture non identificano da sole la causa. La correzione dell'identità non risolve M0-01 e non viene adottata nel prodotto; la ricompilazione aggiuntiva è rimossa dalla CI ordinaria. Una prova locale separata che ometteva l'aggiornamento anticipato delle righe Cocoa sui segnali di inserimento/rimozione ha fallito cinque stadi su nove contro sei del bundle normale in una singola esecuzione: differenza non conclusiva, variante scartata e non committata. Restano necessari diagnosi dei riferimenti AX e collaudo VoiceOver reale.

## English

**Status: open M0-01 defect.** On 2026-09-24, the trial used a **private copy** of the historical `3598b5f` bundle with the current Cocoa patch, external RPATHs removed and ad hoc signature verified, on Apple Silicon macOS 26.6.2. The normal app ran with a temporary `HOME` and synthetic fixtures only. The AX client was already trusted (`AXIsProcessTrusted=true`); no system preference was changed. The original bundle retained a valid signature, and the personal language preference hash stayed `d2bc81b3c6edc55e4b775d89ba679708610c7a417d45c696819ea8a73620ca82`.

The sequence driven through macOS `AXUIElement` was repeated on two launches, leaving each state stable for at least 0.35–1 s:

| Step | AX table | First row |
| --- | --- | --- |
| Press “Load 10,000 examples” through `AXPress` | 10,000 rows, 4 columns | `AXRow`, 4 `AXCell` children, ID `DEMO-00001` |
| Set filter `DEMO-10000` through `AXValue` | 1 row, 4 columns | `AXRow`, first cell `DEMO-10000` |
| Set `no-such-fixture` | 0 rows, 4 columns | No row |
| Clear the filter | 10,000 rows, 4 columns | First-row `AXRole` read returns **−25202**, `kAXErrorInvalidUIElement` |

A fresh AX client process confirmed the invalid reference while the app was idle. Setting AX focus on the table succeeded but did not restore it. The internal `QAccessibleTableInterface` self-test still passes after the same filter sequence; therefore it does not prove that OS bridge references remain valid. A 30-second/10-cycle Cocoa soak with concurrent AX reads ended without a crash, but reads during rapid resets are not an atomic row check.

Three hypotheses were built and signed **only in private Cocoa plugin copies**: distinct identity/hash for synthesized rows; `NSAccessibilityRowCountChangedNotification` on model reset; and directly returning the synthesized row array. The first two passed the internal self-test but **did not** fix the invalid AX reference. The third was tested while app activation was unstable: the abnormal AX tree and focus failures cannot confidently be attributed to that modification. No variant entered the repository or original bundle. In later trials the same AX session sometimes returned `AXApplication` in place of `AXWindow` for newly started processes. A subsequent UI check reported that the Mac was locked; the exact time it locked is unknown. Those later anomalies are therefore inconclusive. Recheck the defect from the first two reproductions with the screen unlocked before deciding on a correction.

Next, repeat the 10,000 → 1 → 0 → 10,000 reset sequence with the Mac unlocked and a fresh AX session; if the defect persists, isolate and fix native-reference lifetime before testing with a real screen reader. Do not close M0-01 based only on row counts, the Qt self-test or the soak.

Commit `4635117` adds `scripts/test-macos-ax-reset.swift`: it launches the bundle with a temporary HOME and checks the first row's role and four cells at rest at each of the four stages. Compilation with local Swift 6.4 passed; the bundle was not run in the locked Mac session. macOS CI runs this as a nonblocking diagnostic: exit 77 means the client lacks Accessibility permission and is **not** a passing result. Even a passing AX result on the runner will not replace VoiceOver on a real desktop.

In [run `4635117`](https://github.com/Matte2599/WebFence/actions/runs/35954825284/job/107490798295), the AX step on the macOS 15 runner **did execute**, without exit 77: four `PASS` results for 10,000/1/0/10,000 rows; the final first row exposes `AXRow`, four cells and `DEMO-00001`. The complete run finished with **six passing jobs**. This validates the CI bundle bridge in that setting but does not resolve the discrepancy with the local trial: both macOS version (15 versus 26.6.2) and bundle (current commit versus historical `3598b5f` copy) differ. The local Mac is still locked; neither a fixed defect nor an OS-specific defect can be inferred.

Git comparison `3598b5f..41256ae` shows no changes under `internal/desktop`, `scripts/qt-cocoa` or `scripts/build-qt-cocoa.sh`; only two linkage-check lines changed in `scripts/package-macos.sh`. This narrows the UI/plugin source differences between trials but does not make the binaries, environments or AX sessions identical.

In [CI `41256ae`, run 35956499419](https://github.com/Matte2599/WebFence/actions/runs/35956499419), **all seven jobs succeeded**, including bundles, Cocoa self-tests, plugin replacement and native materials on both macOS runners. The AX diagnostic is `continue-on-error` and **failed on both**: [macOS 15](https://github.com/Matte2599/WebFence/actions/runs/35956499419/job/107495836991) passed the 10,000, 1 and 0 row stages, then received `AXError -25202` reading the restored first row's role; [macOS 26](https://github.com/Matte2599/WebFence/actions/runs/35956499419/job/107495837015) passed 10,000 rows but found four cells with an empty ID title rather than `DEMO-10000` after filtering to one. Both AX clients executed rather than exiting 77. Success of other checks is not an accessibility pass; the earlier macOS 15 pass is not consistently reproducible. Next, isolate cell lifetime/value and retry with the Mac unlocked, then fix the bridge before closing M0-01.

Commit `b66aefd` runs three filter cycles and snapshots AX attributes after each error. On the [macOS 15 runner](https://github.com/Matte2599/WebFence/actions/runs/35959021694/job/107503379369), the trial starts with 10,000 valid rows. In cycle one the one-row and zero-row filters pass, but the restored first-cell ID fails: `AXRows=10000` and four cells are present, while cell role/title/value all return `AXError -25202`. In cycle two even the one-result row role fails (`-25202`); zero results pass and restoration again produces the invalid cell. In cycle three the one-result and zero-result filters pass, while the restored cell fails again. Four failed stages in all, without exit 77. This is an element-reference validity problem, not merely a choice of textual attribute. The diagnostic does not fail the overall job.

On the [macOS 26 runner in the same run](https://github.com/Matte2599/WebFence/actions/runs/35959021694/job/107503379258), the initial table passes, but **six of nine stages** fail across three cycles: at the first one-row filter the ID title is empty and cell role/title/value in a fresh snapshot return `-25202`; after the first restoration even the row is invalid. In the next two cycles both the one-result and restored rows are invalid (`-25202`); the zero-result filters pass. The AX client executed rather than exiting 77. Both OS versions exhibit invalid references, at different frequencies. This diagnostic still uses the pre-proxy model; the application change remains to be measured.

[CI `18c513e`, run 35961360935](https://github.com/Matte2599/WebFence/actions/runs/35961360935) tests the stable source model with `QSortFilterProxyModel`: while filtering, the Qt self-test confirms row insertions/removals and no `ModelReset`. Debian packages, Windows and both native Linux jobs pass. Cocoa diagnosis shows **no verified improvement**: [macOS 15](https://github.com/Matte2599/WebFence/actions/runs/35961360935/job/107510426255) fails four stages and [macOS 26](https://github.com/Matte2599/WebFence/actions/runs/35961360935/job/107510426348) six, as in the preceding comparison. On 15 the first restored `AXRow` is exposed but its cells are invalid; in later cycles row references also become invalid. On 26 the first one-row filter exposes a row with invalid cells, and restoration invalidates the row. Replacing full resets with row signals alone is insufficient to resolve M0-01. A focused Qt/Cocoa bridge investigation and a real VoiceOver trial are required.

The [completed `18c513e` run, attempt two](https://github.com/Matte2599/WebFence/actions/runs/35961360935) has **seven green jobs**: only macOS 26 failed in attempt one because of a source-mirror timeout after the Cocoa checks. The rerun also completed native materials. The [rerun macOS 26 AX diagnostic](https://github.com/Matte2599/WebFence/actions/runs/35961360935/job/107516319443) still fails **six of nine stages**, with the same `-25202` cell and row sequence; it remains nonblocking. Green CI does not establish accessibility.

[CI `2904f5f`, run 35965326044](https://github.com/Matte2599/WebFence/actions/runs/35965326044) compares a **non-distributable** Cocoa variant in a signed private bundle copy: `accessibilityRows` returns the synthetic row array directly. The source patch applies without fuzz to the verified Qt 6.11.2 archive; both runners build the variant and verify signing, self-test and soak. The earlier local self-test remained inconclusive because of focus in the locked Mac session. On the [macOS 15 runner](https://github.com/Matte2599/WebFence/actions/runs/35965326044/job/107522517243), the normal bundle and variant each fail **three of nine stages**; the precise stages differ, consistent with previously observed intermittence. On the [macOS 26 runner](https://github.com/Matte2599/WebFence/actions/runs/35965326044/job/107522517116), both fail **six of nine stages** in the same sequence: invalid cell after the first one-row filter, then invalid rows on restoration and later filters. The AX client ran rather than exiting 77. Returning rows directly **does not resolve the defect**; the variant remains available only for manual reproduction, and the extra rebuild is removed from routine CI. No product Cocoa change was adopted; M0-01 remains open pending an unlocked Mac and real VoiceOver trial.

[CI `a8b65f2`](https://github.com/Matte2599/WebFence/actions/runs/35966364447) completed with seven green jobs after the direct-row variant was removed; both normal AX diagnostics remain nonblocking and fail. Qt 6.11.2 uses the same `axid` for all synthesized elements of one table; `isEqual` compares only `axid` and synthesized role, while `hash` uses only `axid`. Row/column indexes do not contribute to native identity. The `--ax-distinct-identity` variant adds them **only in a private copy**. The combined patch applies without fuzz; local compilation and signing passed, and Go build ID and originals were preserved. The local self-test failed focus checks during the locked session; a separate Cocoa soak passed four cycles/12.006 s.

[CI `0a568ff`, run 35967360307](https://github.com/Matte2599/WebFence/actions/runs/35967360307) has **seven green jobs**. On the [macOS 26 runner](https://github.com/Matte2599/WebFence/actions/runs/35967360307/job/107528861229), the private variant builds and passes signing, self-test and soak, but the three-cycle AX client fails **six of nine stages**, as does the normal bundle in the same run: an invalid cell after the first one-row filter, followed by invalid rows on restoration and subsequent filters (`-25202`). The client ran rather than exiting 77. `CFEqual` between first and last row is already `false` in the normal bundle and variant; their `CFHash` values also differ, so these readings alone do not identify the cause. The identity change does not resolve M0-01 and is not adopted in the product; the extra rebuild is removed from routine CI. A separate local trial skipping eager Cocoa row updates on insertion/removal signals failed five of nine stages versus six for the normal bundle in a single execution: inconclusive difference, variant discarded and not committed. AX reference investigation and real VoiceOver testing remain necessary.

## Aggiornamento 2026-09-24 / Update 2026-09-24: pool stabile

**IT.** Con `qt.accessibility.table` attivo nella copia privata, i filtri ricreavano array di 10.000/1/0 righe native. Conservare senza limite gli array vecchi ha superato due prove brevi da tre cicli, ma non ha un limite di memoria; conservarne quattro ha fallito due stadi su nove. Un pool di elementi di riga riutilizzati, limitato al massimo numero di righe della tabella, insieme a segnaposto di cella stabili che risolvono l'interfaccia Qt corrente, ha superato due prove private da tre cicli e poi 30 cicli: 92 stadi AX superati, zero fallimenti. Una seconda prova di 30 cicli ha campionato RSS 93 volte: mediana primi 10 169.248 KiB, ultimi 10 160.736 KiB, minimo 127.312, massimo 180.144. È una finestra breve, non una garanzia di memoria a lungo termine. Focus e `AXPress` sulla prima cella hanno restituito successo in una prova separata di tre cicli. Il test di hit-testing non ha dato un risultato interpretabile e non viene contato.

La patch consolidata in `scripts/qt-cocoa/accessibility.patch` è stata applicata senza fuzz a sorgenti Qt 6.11.2 puliti e confrontata con la sorgente privata provata. Un bundle WebFence locale ricostruito con quella patch ha superato firma `codesign --verify --deep --strict`, self-test Cocoa e il client `scripts/test-macos-ax-reset.swift` compilato in Swift: 30 cicli completi, incluse lettura delle quattro celle, focus e pressione iniziale. Il client passa per default tre cicli e accetta 1–100; ora il test CI è bloccante. Questi risultati sono locali su macOS 26.6.2; la [CI `e2cce25`](https://github.com/Matte2599/WebFence/actions/runs/35975333332) supera il test AX bloccante su macOS 15/26; VoiceOver reale resta da verificare. M0-01 rimane aperto.

**EN.** With `qt.accessibility.table` enabled in the private copy, filtering recreated native 10,000/1/0-row arrays. Unbounded retention of old arrays passed two short three-round trials but has no memory bound; retaining four old arrays failed two of nine stages. A row-element pool reused up to the table's largest row count, together with stable cell placeholders resolving the current Qt interface, passed two private three-round trials and then 30 rounds: 92 AX stages passed, zero failures. A second 30-round trial sampled RSS 93 times: first-ten median 169,248 KiB, last-ten median 160,736 KiB, minimum 127,312 and maximum 180,144. This is a short window, not a long-term memory guarantee. Focus and `AXPress` on the first cell succeeded in a separate three-round trial. The hit-test attempt was not interpretable and is not counted.

The consolidated `scripts/qt-cocoa/accessibility.patch` applied without fuzz to clean Qt 6.11.2 sources and matched the private trial source. A locally rebuilt WebFence bundle with that patch passed `codesign --verify --deep --strict`, the Cocoa self-test and the compiled Swift `scripts/test-macos-ax-reset.swift` client: 30 complete rounds, including four-cell reads, initial focus and press. The client defaults to three rounds and accepts 1–100; the CI test is now blocking. These are local macOS 26.6.2 results; [CI `e2cce25`](https://github.com/Matte2599/WebFence/actions/runs/35975333332) passes the blocking AX test on macOS 15/26; actual VoiceOver remains to be verified. M0-01 remains open.
